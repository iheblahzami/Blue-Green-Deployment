name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Deploy
        shell: 'bash --noprofile --norc -e -o pipefail "{0}"'
        run: |
          chmod +x deploy.sh
          ./deploy.sh
